# H2 Classical Detector Dyadic Dichotomy Immutable Evidence

Date: 2026-07-30

Campaign: `LITERATURE-20260730-H2-CLASSICAL-DETECTOR-DYADIC-DICHOTOMY-01`

Classification: `FULL_SUCCESS / SOURCE_DYADIC_DICHOTOMY_FORMALIZED`

## Frozen implementation

- commit: `207953d7cff153eddc017a7d2e2612a786a0c050`;
- Lean Action run: `30491308421`;
- build job: `90709585747`;
- result: passed in `2m18s`;
- local full build before publication: `8804/8804`.

The frozen files are:

```text
LeanLab.lean
LeanLab/Riemann/ClassicalZeroDetectorDyadicDichotomy.lean
LeanLab/Riemann/Targets.lean
LeanLab/Riemann/TargetChecks.lean
LeanLab/Riemann/AxiomsAudit.lean
```

Their diff from the implementation commit is empty at evidence publication.

## Certified endpoint

`classicalDetectorDyadicDichotomy_endpoint` packages:

- the actual divisor-cardinality coefficient estimate;
- the exact actual head/middle/far-tail cutoff;
- exact binary-logarithmic fibers and source block ranges;
- the explicit exponentially small actual far tail;
- the source head bound and uniform Gamma-decaying retained residue;
- the logarithmic binary block-count comparison;
- eventual admissibility of the literal rounded Maynard--Pratt scales;
- the final actual-zero Type-I/Type-II disjunction.

Nine exact TargetChecks compile. Nine selected axiom prints use only `propext`,
`Classical.choice`, and `Quot.sound`.

## Historical finding and boundary

The finite detector paragraph hides no assumed tail, abstract remainder, or simple-zero
premise. The literal source cutoff has quantitative slack but is fully admissible.

The next open producers are rarity estimates for the actual Type-I dyadic blocks and actual
Type-II shifted Mellin integrals. This evidence certifies no zero-density exponent, zero-free
region, H2, or RH.
