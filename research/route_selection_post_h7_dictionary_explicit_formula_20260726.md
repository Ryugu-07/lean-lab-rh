# Route Selection after the H7 Finite Dictionary Explicit Formula

Date: 2026-07-26

Status: `H1_SELECTED / PREREGISTRATION_PUBLIC_CI_REQUIRED`

Parent closure: `31362f4044e99651d7567f91dc4fd8a701974f38`, public Lean Action run
`30187802034`, build job `89755512303`, passed in `1m29s`.

## Selection rule

The historical-door program searches for omitted inferences and weakened premises, not merely
unformalized definitions. A successor must have a fixed source theorem, actual compiled objects
on both sides of its missing edge, and a result that materially changes the route's dependency
graph. Original conjectures and direct RH attacks remain open, but no unproved statement becomes
a premise.

## Fresh comparison

| candidate | compiled frontier | exact missing object | decision |
| --- | --- | --- | --- |
| H1 Bettin--Gonek moment-to-power bridge | The actual mollifier Mellin identity, standalone inverse Mellin transform, compact support, convolution `(2.4)`, contour shift `(2.5)`, residue scale, and exponent consumer compile. | Derive the selected-zero power obstruction from `FarmerLongMollifierBound`, including Cauchy--Schwarz, real-to-integer cutoff control, integration in `t`, and uniform asymptotics. | **Selected.** The full source bridge is now one fixed analytic assembly, and the `[0,T]` theorem admits a new fixed-low-height positive-mass attack. |
| H12 Levinson--Montgomery counts | The derivative divisor, actual counts, exact-count localizer, and conditional Speiser consumer compile. | Functional-equation boundary sign, an indented critical-line contour, top selection, low-height verification, and the count dichotomy. | High-value reserve. The new Jensen selector may help the top edge, but it does not yet remove the indented contour and sign package. |
| H2 half-isolated detector | The finite-line extremum theorem and a symmetry-only bow countermodel compile. | An arithmetic detector excluding bow configurations for actual zeta zeros. | Open reserve; no primary-source amplifier beyond the known finite-real-part hypothesis is identified. |
| H10 function-field transfer | Finite spectral rigidity and the ordinary infinite-trace obstruction compile. | A number-field cohomology or regularized-trace object with a uniform tail. | Structural reserve; no source theorem supplies the transfer. |
| H7 finite Weil dictionary | The exact finite explicit formula and source normalization compile. | Unconditional positivity, inverse/density, cutoff limits, and simple-even ground-state control. | Remains top-level valuable, but the next edge carries essentially RH-strength content and no newly verified source mechanism. Do not continue from API momentum alone. |

## Selected campaign

Select
`LITERATURE-20260726-H1-BETTIN-GONEK-MOMENT-TO-POWER-BRIDGE-01` at node
`H1-BETTIN-GONEK-MOMENT-TO-POWER-BRIDGE-01`.

The fixed primary endpoint is

```text
0 < theta ->
FarmerLongMollifierBound theta ->
forall rho, IsNontrivialZero rho ->
  BettinGonekPowerObstruction theta rho.re.
```

The aggregate corollary must discharge the currently abstract bridge in
`farmerThetaInfinityConjecture_implies_riemannHypothesis`, leaving
`FarmerThetaInfinityConjecture` as the only premise.

## New attack angle

The previous route ledger expected a full critical-line zeta second-moment lower bound. For
Theorem 1, whose moment interval is `[0,T]`, that is stronger than necessary. On a fixed compact
interval beginning at zero:

1. `zeta(1/2+it)` is continuous and nonzero at `t=0`, so its squared norm has a fixed positive
   integral;
2. the selected-zero residue scale has a uniform positive lower bound;
3. the inverse Mellin kernel bound is translation-invariant in `t`;
4. the actual moment on this compact interval is bounded by the assumed moment on `[0,T]`.

The continuous real-cutoff integral in `(2.4)` is partitioned into unit intervals. The compiled
real-cutoff interpolation bounds each interval by its two neighboring integer mollifiers.
Finite Cauchy--Schwarz then converts the source integral to the integer moments controlled by
`FarmerLongMollifierBound`.

This is a materially new re-entry after the inverse-Mellin campaign. It attempts the complete
source theorem, not another helper-only endpoint.

## Claim boundary

Success formalizes Bettin--Gonek Theorem 1 and the implication from Farmer's theta-infinity
conjecture to RH. It does not prove Farmer's conjecture or RH unconditionally. The theorem is a
known conditional bridge; the open long-mollifier estimate remains unavailable as a premise.

No production Lean source, Target, TargetCheck, or axiom-audit entry may be edited until the
docs-only preregistration passes public Lean Action CI. The six inherited user/exposure files
remain untouched and unstaged.
