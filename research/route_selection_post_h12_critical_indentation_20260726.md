# Route Selection after H12 Critical Indentation

Date: 2026-07-26

Status: `D9_CONREY_LI_PHASE_OBSTRUCTION_SELECTED`

## Closed parent

Campaign
`LITERATURE-20260726-H12-LEVINSON-MONTGOMERY-CRITICAL-INDENTATION-01`
closed publicly as `FULL_CRITICAL_INDENTATION_SUCCESS`:

- preregistration commit `54b5eabdf46acf44878db80cf2e38657f7fb7378`, run
  `30193955050`, job `89772029846`;
- frozen implementation `49d43eda415c00c10939c2df529b6231c973aa5b`, run
  `30195029807`, job `89774903553`;
- immutable evidence `482835012cba3c51839d428a41127fe40e513e2e`, run
  `30195156406`, job `89775241280`;
- final ledger `4ee3b000bf2baff304edfedc215cf9143b399cea`, run
  `30195238711`, job `89775469902`.

The next H12 edge is no longer a local omitted lemma: it requires a certified bottom contour,
cofinal admissible top contours, a global indented argument principle, and the full count
comparison.

## Cross-family comparison

- `H1/D3`: the exact Bettin--Gonek implication is compiled and now stops at Farmer's open
  arbitrary-length mollified moment conjecture. The 2025 short-mollifier variational mechanism
  is also compiled.
- `H2/D4` and `H11/D5`: finite bow and persistent sparse-exception models are compiled, but no
  source-backed arithmetic amplifier eliminating one actual exceptional orbit is known.
- `H7/D6` and `H10/D7`: finite spectral and function-field rigidity mechanisms are compiled;
  the missing number-field spectral/cohomological object and regularized tail are not supplied
  by the sources.
- `D9`: the Suzuki generic limit interpretations were audited, but the older Conrey--Li
  obstruction named in the canonical atlas has not been reconstructed. Its final Sarnak remark
  gives a nonnumerical phase-crossing proof whose topological core is bounded and independently
  testable.

## Selection

Selected campaign:
`LITERATURE-20260726-D9-CONREY-LI-PHASE-OBSTRUCTION-01`.

Selected node: `D9-CONREY-LI-PHASE-OBSTRUCTION-01`.

The source computation at `t=282` is not accepted as proof evidence. Instead, formalize the
source's nonnumerical mechanism:

```text
dense logarithmic values
  + uniformly bounded imaginary correction
  + continuous corrected phase on a connected domain
  -> the corrected phase takes the value pi
  -> the xi ratio has negative real part
  -> its reciprocal has negative real part
  -> the Conrey--Li shifted-kernel nonnegativity consequence fails.
```

The actual zeta specialization remains conditional until the known value-distribution theorem,
the continuous logarithm branches, and the bounded archimedean correction are formalized. This
is a historical obstruction reconstruction, not a proof or disproof of RH.

## Ranking reason

This target supplies breadth and tests whether a published failed route contains a logical gap.
It is preferred over another H12 local lemma, another numerical H6/H7 optimization, or a generic
density countermodel. Success will either validate the exact phase-crossing logic or expose a
missing continuity/connectedness hypothesis in the Sarnak sketch.

The persistent RH Goal remains active.
