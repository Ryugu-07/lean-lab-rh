# Route Selection after H7 Volterra Source Calculus

Date: 2026-07-24

Status: `H7_FINITE_DICTIONARY_ADMISSIBILITY_SELECTED / PREREGISTRATION_CI_REQUIRED`

## Closed parent campaign

Campaign `LITERATURE-20260724-H7-WEIL-FINITE-DICTIONARY-01` is publicly closed at final-ledger
commit `46befa6a2e935e73b077140e5e9df24df3623db6`, public Lean Action run `30073083407`, build job
`89417854356`, in `1m39s`. It proves the literal finite Volterra source calculus and actual
finite-prime transport. The zero-side dictionary, H7, and RH remain open.

## Source correction

Groskin, arXiv:2607.02828, Lemma 2.2 does not require moment-neutrality for admissibility.
For every finite even-sector vector, the induced test is claimed to be even, entire of finite
exponential type, compactly band-limited, and

```text
g_v(z) = O((1 + |Re z|)^-2)
```

uniformly on each fixed horizontal strip. The Fourier weight vanishes continuously at the two
band endpoints; its derivative may jump there or at the origin. The source handles the second
integration by parts in the Stieltjes sense because the zero extension of the derivative has
bounded variation.

This corrects the preliminary hypothesis that extra moments were needed merely to enter the
source's Guinand--Weil class. Moments remain relevant to pole-neutral subfamilies, not to basic
admissibility.

## Cross-route ranking

| candidate | exact open edge | information gain | verdict |
| --- | --- | --- | --- |
| H7 finite dictionary | The compiled finite Fourier weight has not been proved continuous at the band boundary, transported to the project's Laplace/xi coordinates, given source-strength horizontal-strip decay, or summed absolutely over actual xi zeros. | Directly tests the missing hypothesis of the paper's exact zero-sum theorem, using both sides already present in the project. | `SELECTED` |
| H1 Bettin--Gonek | Inverse Mellin support, convolution `(2.4)`, and moment transfer remain after equations `(2.1)` and `(2.5)`. | High direct-RH value, but the next edge is a broader analytic package than the bounded H7 admissibility lemma. | `OPEN / NEXT RERANK` |
| H12 Speiser | The actual argument-principle count equivalence remains open. | Historically important and distinct, but requires a new meromorphic-count infrastructure package. | `OPEN / NEXT RERANK` |
| H2 bow exclusion | No actual arithmetic localizer currently amplifies one off-line bow. | A generic symmetry-only mechanism was already falsified. | `OPEN / SOURCE SEARCH` |
| H10 function field | Finite spectral rigidity compiles, while ordinary reciprocal-trace transfer was falsified. | Another finite polynomial root-count lemma would not name a number-field consumer. | `MONITOR` |
| H11 sparse exceptions | Pair correlation remains horizontally multiplicity-blind. | Needs a named arithmetic amplifier before another formal proof campaign. | `OPEN / SOURCE SEARCH` |

## Why this is not numerical optimization

The selected edge has no adjustable constant or proportion endpoint. It asks whether the actual
finite vector built in Lean produces a legitimate explicit-formula test at every complex point
and an absolutely convergent series on the actual multiplicity-bearing xi divisor. Success moves
the historical finite matrix from the prime side to the threshold of the zero side. Failure
identifies whether the source's BV/Stieltjes argument, coordinate normalization, or project
zero-count API is the obstruction.

## Fixed next campaign

Preregister
`LITERATURE-20260724-H7-WEIL-FINITE-DICTIONARY-ADMISSIBILITY-01` at node
`H7-WEIL-FINITE-DICTIONARY-ADMISSIBILITY-01`.

The endpoint reconstructs the source test, proves boundary continuity and compact support,
aligns it with the project's bilateral Laplace and xi-zero coordinates, proves the source
horizontal-strip inverse-square decay, and proves absolute summability over the actual xi
divisor with multiplicity.

The generalized Guinand--Weil arithmetic explicit formula for this weaker piecewise-smooth/BV
class is not silently inherited from the project's compact `C^6` theorem. It remains a separate
downstream edge unless derived in Lean.

## Rotation rule

After this campaign's public closure, rerank H1 inverse Mellin/convolution, H12 Speiser counting,
H2 arithmetic bow localization, and the remaining H7 zero/pole/archimedean transports. Do not
select another H7 campaign merely because its local API is warm. An H7 successor must expose a
new source theorem or compose the admissibility result into the actual zero-side identity.

