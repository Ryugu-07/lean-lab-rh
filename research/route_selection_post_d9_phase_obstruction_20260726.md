# Route Selection after D9 Conrey--Li Phase Obstruction

Date: 2026-07-26

Status: `H10_BOMBIERI_STEPANOV_FROBENIUS_AUXILIARY_SELECTED`

## Closed parent

Campaign `LITERATURE-20260726-D9-CONREY-LI-PHASE-OBSTRUCTION-01` is fully public-green:

- preregistration `adc7b306fb045185c8edef802218b3fa745d5f04`, run `30195518860`,
  job `89776251261`;
- frozen implementation `74787a77a20218bb967d18279b29bd7ab9a5ab97`, run `30195816933`,
  job `89777044355`;
- immutable evidence `0f8cd8437a8495bc57be0c556b74d95bc7bef623`, run `30195949149`,
  job `89777408452`;
- final ledger `fca9616b7580eeff45b7591a66eb061cf4a94af9`, run `30196032626`,
  job `89777631633`.

The conditional phase-crossing logic is valid. Actual log-zeta value distribution, logarithm
branches, bounded phase correction, and the RKHS positivity bridge remain open.

## Cross-family comparison

- `D3/H1`: the exact Bettin--Gonek chain is compiled through its consumer and stops at the open
  arbitrary-length mollified moment estimate.
- `D4/H2` and `D5/H11`: bow and sparse-exception countermodels are compiled; no source-backed
  arithmetic amplifier for the last actual off-line orbit is available.
- `D6/H7`: finite Weil dictionaries and source calculus are deeply formalized; positivity,
  inverse-density limits, and the actual number-field spectral object remain open.
- `H12`: the next Levinson--Montgomery edge is a global contour-count package rather than a
  plausible omitted local identity.
- `D9`: the newly closed campaign validates the old phase obstruction but reaches a known
  value-distribution and RKHS frontier.
- `D7/H10`: the project has formalized the last finite power-sum rigidity step and has refuted an
  ordinary infinite-trace transfer, but it has not reconstructed the central Frobenius auxiliary
  function mechanism in the successful Bombieri--Stepanov proof.

## Selection

Selected campaign:
`LITERATURE-20260726-H10-BOMBIERI-STEPANOV-FROBENIUS-AUXILIARY-01`.

Selected node: `H10-BOMBIERI-STEPANOV-FROBENIUS-AUXILIARY-01`.

The source mechanism is:

```text
base B = sum_i v_i * s_i^r
characteristic-p power m = p^mu
auxiliary F = B^m = sum_i v_i^m * s_i^(r*m)
descent delta(F) = sum_i v_i^m * s_i
r*m = #K and a in K -> s_i(a)^(r*m) = s_i(a)
delta(F) = 0 -> F vanishes at every K-point
F is an m-th power -> every such zero has multiplicity at least m
sum of root multiplicities <= degree -> #points <= degree(F)/m.
```

Bombieri's curve proof obtains the nonzero kernel element and pole budget from Riemann--Roch,
polar expansions, and a tensor-product injectivity statement. The fixed campaign formalizes the
finite-field algebra and multiplicity budget while keeping those geometric inputs outside.

## Ranking reason

This is the least explored proof-construction layer among the five broad historical families.
It is also a sharp omission test: the finite-field identity and perfect-power multiplicity are
often summarized in one sentence, while nonzero kernel production is a distinct and indispensable
step. A compiled polynomial model can show exactly which part is formal algebra, which part can
saturate, and which part has no characteristic-zero number-field analogue.

The campaign does not claim a number-field transfer, a curve-level Hasse--Weil bound, or progress
on RH. The persistent RH Goal remains active.
