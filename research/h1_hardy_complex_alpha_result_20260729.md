# H1 Hardy Complex-Alpha Equation Result

Date: 2026-07-29

Campaign: `LITERATURE-20260729-H1-HARDY-COMPLEX-ALPHA-01`

Node: `H1-HARDY-COMPLEX-ALPHA-EQUATION-TWO-01`

Classification: `FULL_SUCCESS / KNOWN_HARDY_1914_COMPLEX_ALPHA_EDGE_FORMALIZED`

Public state: `FINAL_LEDGER_PUBLIC_GREEN / CLOSURE_RECEIPT_PENDING`

## Compiled result

The new 1,318-line no-sorry module `LeanLab/Riemann/HardyComplexAlpha.lean` proves:

1. the exact critical-line Gamma bound retaining `exp (-(pi / 2) * |t|)`;
2. polynomial transport of that decay from Gamma real part `1/2` to `1/4`;
3. integrability of the actual Hardy Xi kernel under every exponential weight
   `a < pi / 2` and every natural polynomial weight;
4. differentiation under the half-line integral and
   `analyticOnNhd_hardyXiInteriorIntegral`;
5. analyticity and exact Jacobi-theta normalization of `hardyThetaAlpha`;
6. the branch-correct imaginary-axis identity `hardyEquationTwoLeft_imaginary`;
7. `hardyEquationTwo` on the full strip `abs (Re alpha) < pi / 2`.

The proven Target is `H1.hardy.complex-alpha-equation-two`.

## Proof mechanism

The compiled polynomial critical-line zeta bound and the exact Gamma decay give the actual xi
kernel enough exponential margin for the whole source strip. The spare margin also absorbs every
polynomial factor generated by local complex differentiation, so the parameter integral is
analytic without assuming an abstract xi-decay interface.

The theta series is identified with Mathlib's one-variable Jacobi theta after proving that
`alpha -> I * exp (I * alpha)` stays in the upper half-plane on the strip. On the imaginary
axis, the principal positive-real power branch, `hardyXi_even`, and the exact coefficient
`4 / (2 * pi) = 2 / pi` convert the compiled positive-real Cahen-Mellin equation (1) into the
two half-line exponentials in equation (2).

Both sides are analytic on the convex strip. They agree at the nonzero points
`I / (n + 1)`, which accumulate at zero, so the analytic identity theorem yields equation (2)
throughout the strip.

## Omission reading

Hardy's displayed transition from equation (1) to equation (2) suppresses several independent
premises: exponential xi domination, parameter-integral analyticity, theta analyticity, the
positive-real branch calculation, even-kernel normalization, and connected-domain analytic
continuation. All six now compile for the project's actual definitions.

This closes a known historical bridge. It does not yet establish Hardy's following
differentiation and tangential boundary-limit argument.

## Audit

- production module: 1,318 lines;
- exact TargetChecks: five;
- selected axiom prints: five, each only `propext`, `Classical.choice`, and `Quot.sound`;
- no `sorry`, `admit`, custom axiom, `native_decide`, `opaque`, or `unsafe`;
- no heartbeat, recursion-depth, or resource relaxation;
- warning-as-error compiles: pass for the module, Targets, TargetChecks, AxiomsAudit, and root;
- full project build: `8795/8795`;
- frozen five-file proof and registration diff: empty;
- protected inherited files remain untouched and unstaged.

## Claim boundary

This result does not prove equation (3), the tangential theta derivative limit,
`HardyXiAbelMomentLaw`, unconditional infinitely many critical-line zeros, any positive
critical-zero proportion, H1, or RH.

Deltas: historical route coverage `+1`, source logic `+1`, hard gap `0`, RH frontier `0`.

The persistent RH Goal remains active.

## Public chain

- Preregistration commit `ef1752a44ca1b3242348e7ac40ac4b50529b0efe` passed Lean Action
  run `30415933876`, build job `90462332932`, in `1m37s`.
- Frozen implementation commit `0f0cb7c2829dd8c35ccf926e0bfb6a79d75147eb` passed Lean Action
  run `30418152861`, build job `90469028889`, in `3m0s`.
- Immutable-evidence commit `389dc3790e2affe3cc6cb7329f78a37cff04023e` passed Lean Action
  run `30418420614`, build job `90469840559`, in `1m56s`.
- Final-ledger commit `1de09f4d05dd114a0eca8b89c45fdb0408e6eda7` passed Lean Action
  run `30418635244`, build job `90470496834`, in `1m37s`.

The closure receipt records this chain and closes only the fixed node.
