# Route Selection After H7 Archimedean Tail Density

Date: 2026-07-23

Parent final ledger: `64782a564a19a8e9c25a0d520bcbbcb2397b807a`

Parent public CI: Lean Action run `29980056767`, build job `89119806051`, passed in `1m36s`.

Global RH Goal: active.

## Historical-mechanism comparison

| candidate | exact next object | present evidence | decision |
| --- | --- | --- | --- |
| H1 Bettin--Gonek | Prove the actual rational `J_t` contour shift `(2.5)`, the uniform boundary-line bound, and the exact selected-residue power term. | The literal mollifier Mellin identity, regularized auxiliary factor, and nonzero selected-pole coefficient are already public-green. Existing project rectangles and improper-integral APIs make the edge bounded. | `SELECT` |
| H7 three-block Weil assembly | Add the actual pole, finite-prime, and archimedean matrices and connect them to a prolate vector. | All three blocks now compile, but assembly alone does not control aggregate prime cancellation, the odd pole loss, the spectral gap, or either uniform cutoff limit. | `OPEN / DEFER_DEFINITION_ONLY_STEP` |
| H12 Levinson--Montgomery | Prove the actual zeta and zeta-derivative rectangle count estimates behind Speiser. | The logical consumer compiles, but the next edge simultaneously needs a meromorphic argument principle, Gamma estimates, indented critical-line contours, and low-height signs. | `OPEN / BROADER_THAN_ONE_CAMPAIGN` |
| H2 Maynard--Pratt | Exclude actual-zeta bow configurations or build a detector effective on them. | The generic bow obstruction is public-green; no source-backed arithmetic bow localizer is currently available. | `OPEN / DISCOVERY_INPUT_NEEDED` |
| H11 pair correlation | Amplify one sparse off-line orbit to a nonvanishing horizontal-multiplicity defect. | The finite hinge and persistent-exception countermodel compile; the required arithmetic amplification is still unidentified. | `OPEN / DISCOVERY_INPUT_NEEDED` |

## Selection

Select campaign `LITERATURE-20260723-H1-BETTIN-GONEK-J-CONTOUR-01`, node
`H1-BETTIN-GONEK-J-CONTOUR-01`.

The source is Bettin--Gonek arXiv:1604.02740, equations `(2.3)`--`(2.5)`. On the right line the
already compiled `H_t` and regularized `G_t` must multiply to the literal rational kernel. That
kernel has exactly one crossed pole between real parts zero and three when the selected zero has
real part at least one half. The campaign will prove the infinite contour identity from finite
rectangles, not assume a generic contour-shift premise.

The endpoint also requires a bound on the real-part-zero line that is uniform in `x>0`, plus the
exact norm growth of the selected residue. These two statements turn the contour identity into the
source lower inequality before convolution and Cauchy--Schwarz.

## Claim boundary

This campaign does not prove vertical decay of `G_t` by itself, inverse Mellin support or
boundedness of `g_t`, the Mellin convolution identity, the upper bound `(2.4)`, Cauchy--Schwarz,
the zeta second-moment lower bound, the complete moment-to-power bridge, Farmer's arbitrary-length
moment conjecture, H1, or RH. It tests one central historical analytic edge and then returns to
cross-route ranking.

Original conjectures and direct RH attacks remain open throughout. The H7 source blocks remain
available for a later total-matrix campaign, and no historical family is marked exhausted.
