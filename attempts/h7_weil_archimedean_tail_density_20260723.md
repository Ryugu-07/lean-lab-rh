# H7 Weil Archimedean Tail Density Campaign

Date: 2026-07-23

Campaign: `LITERATURE-20260723-H7-WEIL-ARCHIMEDEAN-TAIL-DENSITY-01`

Selected node: `H7-WEIL-ARCHIMEDEAN-TAIL-DENSITY-01`

Status: `IMPLEMENTATION_PUBLIC_GREEN / IMMUTABLE_EVIDENCE_REQUIRED`

## Attempt log

| phase | action | result | next decision |
| --- | --- | --- | --- |
| `ROUTE_SELECTION` | Compared H1 inverse Mellin, H7 archimedean source, true ground-state/prolate comparison, actual Conrey flat-prefix exclusion, and H10/H11 open transfer mechanisms. | The H7 archimedean density is the last actual source block missing from the shared finite matrix API. H1's next edge remains higher-level but couples four analytic infrastructures. | Select a bounded actual-source archimedean endpoint, then rerank across families. |
| `SOURCE_AUDIT` | Re-read arXiv:2607.02828 equation `arch-source` and Theorem 3.2 against arXiv:2511.22755 equations `thetaprime`, `weinfty`, and `computearch`. | The source fixes the `1/pi^2` derivative normalization, both node and diagonal formulas, and the positive rank-two Cauchy density. The diagonal is a stated nontrivial guard. | Make the literal interval source mandatory. |
| `DEPENDENCY_AUDIT` | Inspected the compiled finite divided-difference, parity, digamma, finite interval, and matrix APIs. | Pole and prime blocks already share the required matrix carrier. Digamma continuity and finite interval integration exist; no compiled theorem currently supplies the published `h_+(T)>0` threshold. | Keep the sign explicit and conditional; do not smuggle in the Arb gate. |
| `PREREGISTRATION` | Fixed literal source samples, rank-two density, reflection, quadratic, and conditional integrated-increment endpoints. | Production Lean remains gated on public preregistration CI. | Commit, push, and require public CI before proof-source editing. |
| `PREREGISTRATION_CI` | Published commit `be0b58b3e27bff4af738d81db6cac3f223f2eee7`. | Lean Action run `29977635904`, build job `89112697896`, passed in `1m28s`. | Open the production proof-source gate. |
| `TRUE_SOURCE_DERIVATIVE` | Differentiated the literal finite interval kernel in `x` under the integral and evaluated it at centered integer nodes. | The diagonal is exactly `(2 sin^2(Lr/2)/rho)(a^2+n^2)/(a^2-n^2)^2`; no factor, sign, or diagonal mismatch occurred. | Continue to actual samples and matrix entries. |
| `MATRIX_IDENTIFICATION` | Built value and derivative samples from the literal source and passed them to `weilFiniteDividedDifferenceMatrix`. | Every entry equals the closed rational formula and the positive-sign rank-two Cauchy sum with coefficient `h_+(r) sin^2(Lr/2)/(pi^2 rho)`. | Prove reflection and all-vector quadratic identities. |
| `DIGAMMA_CONTINUITY` | Reconstructed continuity of `h_+` from complex Gamma analyticity, analytic differentiation, and Gamma nonvanishing on `Re(z)>0`. | No extra measurability or interval-integrability premise is needed for the source increment. | Exchange finite contractions with interval integration. |
| `INTEGRATED_INCREMENT` | Defined the entrywise interval increment and moved its finite quadratic contraction through the integral. | Its quadratic value is the integral of the pointwise two-square density; it is nonnegative under the explicit premise `0<=h_+` on the interval. | Run mechanical gates and publish the frozen implementation. |
| `LOCAL_GATES` | Ran direct warning-as-error compile, 12 exact TargetChecks, 11 selected axiom prints, forbidden-token scan, `git diff --check`, and full build. | New declarations use only `propext`, `Classical.choice`, and `Quot.sound`; scan is empty; full build passes `8756/8756`. | Freeze implementation in a commit and require public CI. |
| `IMPLEMENTATION_CI` | Published frozen implementation commit `9546806d8c3d0afeef9f6c7ee674982e8710576a`. | Lean Action run `29979643215`, build job `89118608592`, passed in `2m32s`; proof source is frozen. | Record immutable coordinates in docs only and require public CI. |

## Known obstacles

- Evaluate the sine-cosine interval kernel at centered integer nodes without dropping the
  `sin^2(L*T/2)` boundary factor.
- Differentiate the actual interval kernel in `x` and evaluate the diagonal exactly.
- Keep `rho=2*pi/L`, `a=T/rho`, and all nonzero denominators aligned under `T>rho*N`.
- Exchange finite matrix contractions with the interval integral without introducing an abstract
  matrix unrelated to the source.
- Preserve the distinction between conditional semidefiniteness and the source's unconditional
  `T>=7` sign theorem.

## Claim boundary

No unconditional `h_+` positivity, strict positive definiteness, total positivity, tail limit,
tail budget, total Weil sign, Herglotz inequality, simple-even ground state, source convergence,
H7, or RH is registered as proven.
