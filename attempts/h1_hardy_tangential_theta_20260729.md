# H1 Hardy Tangential Theta Attempts

Date: 2026-07-29

Campaign: `LITERATURE-20260729-H1-HARDY-TANGENTIAL-THETA-01`

Node: `H1-HARDY-TANGENTIAL-THETA-LIMIT-01`

Status: `FULL_SUCCESS / IMPLEMENTATION_PUBLIC_GREEN / IMMUTABLE_EVIDENCE_PUBLIC_GREEN / FINAL_LEDGER_PUBLIC_GREEN / CLOSURE_RECEIPT_CI_PENDING`

## Fixed question

Can the compiled Hardy equation (2) be differentiated and taken to the source's tangential cusp
to construct `HardyXiAbelMomentLaw` unconditionally and thereby prove infinitely many actual
critical-line zeros?

## Attempt ledger

| round | mode | observation | decision |
| --- | --- | --- | --- |
| 1 | `ROUTE_SELECTION` | H2, H7/H8, H10, H11, H12, and H14 remain live, but their first producers require broad global estimates or concrete objects. Hardy equation (2) now lies immediately beside one explicit missing law whose compiled consumer yields an unconditional historical theorem. | Select the tangential theta edge after fresh cross-family comparison, not by H1 inertia. |
| 2 | `SOURCE_FACSIMILE` | Hardy's actual equation (3) contains `t^(2p)`, approaches `alpha=pi/2` tangentially, and invokes Bohr--Riesz summability to kill every theta derivative. | Lock the exact source constant and left Abel filter; do not substitute endpoint integrability. |
| 3 | `LIBRARY_AUDIT` | Mathlib already proves the two-variable Jacobi theta functional equation, translation, evenness, quasi-periodicity, and upper-half-plane differentiability. The project proves full-strip polynomial xi domination. | Attack the source limit through a branch-checked cusp-to-infinity theta transformation and all-order Gaussian flatness. |
| 4 | `PREREGISTRATION` | Full success, meaningful partial, falsification controls, and the exact downstream theorem are fixed. | Publish docs only and await public CI before any production Lean edit. |
| 5 | `PUBLIC_GATE` | Preregistration commit `648d8e8140f1af0ea5726cf030b8ab4bc4dc8581` passed Lean Action run `30429533400`, build job `90503309053`, in `2m43s`. | Open production without changing the fixed endpoint or source constant. |
| 6 | `CUSP_TRANSFORM` | Translation from the cusp `-1` followed by the branch-explicit Jacobi functional equation gives Hardy's exact boundary term as a principal square-root prefactor times a half-integer theta series at `-1/(tau+1)`. Along the real path its real part is `-1/2` and imaginary part tends to `+infinity`. | Replace the source's general Bohr--Riesz invocation with this exact cusp-to-infinity identity. |
| 7 | `ALL_ORDER_THETA` | A Cauchy estimate on the disk of radius `Im(tau')/2` proves exponential decay of every iterated derivative of the half-integer theta series after every fixed polynomial loss. The principal `cpow` branch is differentiable on the proved slit-plane image. | Encode derivatives through a finite basis closed under `u'=-I*u*(1+u)` and the prefactor ODE; prove every model term tends to zero. |
| 8 | `THETA_FLATNESS` | The recursive finite derivative model compiles equal to every actual `iteratedDeriv`; each term is dominated by the all-polynomial Gaussian theorem. | Compile `tendsto_iteratedDeriv_hardyThetaBoundaryTerm_all` for every natural order. |
| 9 | `ACTUAL_INTEGRAL` | Local spare strip width dominates every next derivative of Hardy's actual Xi kernel by `2*|t|^(m+1)*hardyXiExponentialWeight a t`. At order `2*p` and real alpha, the explicit kernel is exactly the source Abel-moment integrand. | Compile all-order differentiation under the half-line integral and `hardyXiInteriorIntegral_iteratedDeriv_real`. |
| 10 | `EQUATION_THREE` | Algebraic rearrangement of compiled equation (2) gives `interior + boundary = pi*cos(alpha/4)`. Local analytic equality permits `2*p` derivatives, and Lean computes the coefficient `pi*(1/4)^(2*p)*(-1)^p`. | Take the left endpoint using theta flatness; retain the literal source filter and constant. |
| 11 | `HISTORICAL_ENDPOINT` | `tendsto_hardyXiAbelMoment_unconditional` and interior integrability construct `hardyXiAbelMomentLaw_unconditional`. Direct composition with the existing sign-amplification consumer compiles `infinite_criticalLineZeros_hardy`. | Classify full success for Hardy's 1914 infinitude theorem, with no quantitative-count claim. |
| 12 | `LOCAL_AUDIT` | The two new modules total 2,489 lines and contain no forbidden declarations or resource relaxations. Four exact TargetChecks compile; six selected axiom prints contain only `propext`, `Classical.choice`, and `Quot.sound`; warning-as-error checks, `git diff --check`, and full build `8797/8797` pass. | Freeze the six implementation files and require public CI. |
| 13 | `PUBLIC_IMPLEMENTATION_GATE` | Frozen implementation commit `75f5c575b2c3f050f0e5703efb5ce6851d97775c` passed Lean Action run `30435633763`, build job `90522592740`, in `2m17s`. | Publish immutable evidence without changing proof source. |
| 14 | `PUBLIC_EVIDENCE_GATE` | Docs-only immutable-evidence commit `85f0ae62feb457961a3e71ca15db50fa195ce459` passed Lean Action run `30436167642`, build job `90524303908`, in `2m7s`; the six frozen files retain an empty diff from implementation. | Publish the final ledger, then a separate closure receipt. |
| 15 | `PUBLIC_FINAL_LEDGER_GATE` | Docs-only final-ledger commit `2365765bf5ec9eb155312dce119fe6cccbbbff56` passed Lean Action run `30436418445`, build job `90525116015`, in `1m44s`; the six-file freeze is intact. | Publish one closure receipt and require its public CI before local STOP. |

## Current frontier

- `compiled_left_endpoint`: `hardyEquationTwo` on `abs (Re alpha) < pi/2`.
- `fixed_campaign_endpoint`: unconditional `HardyXiAbelMomentLaw` and infinitely many actual
  critical-line nontrivial zeros; both now compile.
- `closed_technical_risks`: all-order branch-correct cusp flatness, all-order actual integral
  differentiation, the equation (3) sign and `4^(2*p)` normalization, and the left Abel limit.
- `strict_boundary`: no quantitative zero count, positive proportion, H1, or RH.
- `protected_files`: the six inherited modified/untracked files remain untouched and unstaged.
- `global_goal`: active.

## Result classification

`FULL_SUCCESS / HARDY_1914_UNCONDITIONAL_INFINITY_FORMALIZED`

This closes the fixed historical source edge and removes the explicit
`HardyXiAbelMomentLaw` premise from the existing contradiction consumer. It proves infinitely
many actual nontrivial zeros on the critical line. It does not prove a growth rate for their
count, a positive proportion, H1, or RH. Historical-route coverage and source-logic deltas are
`+1`; RH-frontier delta is `0`.

## Loop metadata

- `model`: Codex, GPT-5 family; exact serving variant is not exposed.
- `reasoning_effort`: not exposed.
- `budget`: no numerical quota; serving budget not exposed.
- `compaction`: inherited summary detected; canonical files and primary facsimile rechecked.
