# H12 Levinson--Montgomery Top Argument Variation

Date: 2026-07-30

Campaign:
`LITERATURE-20260730-H12-LEVINSON-MONTGOMERY-TOP-ARGUMENT-VARIATION-01`

Status: `FULL_FIXED_ENDPOINT_SUCCESS / LOCAL_AUDIT_GREEN /
PUBLIC_IMPLEMENTATION_CI_REQUIRED`

## Attempt log

| step | mode | result | next action |
| --- | --- | --- | --- |
| 1 | `PARENT_CLOSURE` | The actual zeta and phase-normalized derivative Jensen divisor counts, with source crossing inclusion, are publicly closed at commit `eab0fafd0e21101a759e9ccb20c1ba3b2ea4494a`. Argument variation remains zero in the result ledger. | Rerank across historical families without adjacency preference. |
| 2 | `CROSS_FAMILY_SELECTION` | H1/H2 require deep global moments; H7 needs an actual Rayleigh-gap rate; H8 needs a concrete Hardy RKHS; H10 needs general-curve Riemann--Roch infrastructure; H11 needs actual PCC/Fujii asymptotics. H12 now has an exact bounded bridge immediately downstream of a newly compiled actual producer. | Select `H12-LM-JENSEN-TOP-VARIATION-01`. |
| 3 | `PRIMARY_SOURCE_AUDIT` | Levinson--Montgomery page 52 suppresses the conversion from Jensen real-part zeros to continuous changes of argument of actual `zeta` and `zeta'`. | Reconstruct the hidden branch and partition logic. |
| 4 | `LEAN_INVENTORY` | The project has actual crossing-to-divisor-support theorems, multiplicity sums, horizontal derivative formulas, and local logarithm infrastructure. Mathlib has principal-argument half-plane bounds and finite sorting, but no packaged crossing-variation theorem. | Freeze a generic finite-crossing bridge and actual cofinal endpoint. |
| 5 | `M0_ALIGNMENT` | Continuous variation is represented by the imaginary part of the horizontal logarithmic-derivative integral. Reversing `1 -> 0` changes sign only. The derivative phase is constant in `sigma`, and the source's remaining `2^sigma` factor is positive real. | Require exact phase cancellation and both actual integrals. |
| 6 | `PREREGISTRATION` | Full success requires the generic finite-crossing theorem, support-cardinality-to-multiplicity charge, actual zeta and derivative instantiations, and cofinal common zero-free heights. | Publish docs-only preregistration and await CI. |
| 7 | `PUBLIC_PREREG_GATE` | Commit `01a51d79a350f4dd4d9a8bf46bd3458b9ec44ff2` passed Lean Action run `30532626405`, build job `90838279704`, in `1m37s`. | Open the frozen production endpoint. |
| 8 | `ONE_GAP_LOG_BRANCH` | Weak right- and left-half-plane endpoint formulas compile. Intermediate value forces one sign on every crossing-free gap, so its continuous argument variation is at most `pi`, including imaginary-axis endpoints. | Consume a finite crossing set without assuming an ordered partition. |
| 9 | `FINITE_CROSSING_RECURSION` | Filtering and strictly sorting an arbitrary finite crossing superset gives a compiler-checked `pi*(card+1)` bound. Endpoint crossings and extraneous set elements are allowed. | Charge actual crossings to Jensen multiplicities. |
| 10 | `DIVISOR_CARDINALITY_CHARGE` | Lean proves real support projection cardinality is at most complex divisor support cardinality, and analyticity makes every support value a positive integer contributing at least one to the finsum. | Instantiate actual zeta and derivative paths. |
| 11 | `ACTUAL_TOP_PATHS` | Common zero-free heights on `[0,1]` occur cofinally. The zeta path gives `zeta'/zeta`; the phase-normalized derivative path has the same quotient `zeta''/zeta'` because the nonzero phase cancels exactly. | Compose the Jensen logarithmic estimates. |
| 12 | `ACTUAL_LOG_BOUND` | Both actual horizontal integral imaginary parts are bounded by one common `C*log(t+2)` at every sufficiently large admissible height, and simultaneously at arbitrarily large admissible heights. | Register exact checks and audit axioms. |
| 13 | `LOCAL_AUDIT` | The 946-line no-sorry module, aggregate import, target registry, nine exact TargetChecks, and eight selected axiom prints pass warning-as-error. Every selected theorem uses only `propext`, `Classical.choice`, and `Quot.sound`; the forbidden scan and `git diff --check` are empty; full build passes `8816/8816` with inherited warnings only. | Freeze and publish the implementation, then require public Lean Action CI before immutable evidence. |

## Current obstruction map

1. Prove one-gap argument variation using a valid right- or left-half-plane logarithm with weak
   endpoint containment.
2. Recursively consume or sort an arbitrary finite crossing superset without assuming a
   supplied partition.
3. Charge the real projection of divisor support to the integer multiplicity sum.
4. Produce common zero-free top heights on the complete source interval `[0,1]`.
5. Compose the additive crossing bound with both actual Jensen `O(log(t+2))` theorems.

## Claim boundary

The no-sorry production module
`LeanLab/Riemann/LevinsonMontgomeryTopArgumentVariation.lean` compiles the generic bridge,
actual support charge, both actual top variations, and the cofinal simultaneous endpoint.
Registration and the full local audit are green. Public implementation CI, immutable evidence,
and closure-ledger CI remain. The global indented argument principle, bottom orientation,
Levinson--Montgomery counts, Speiser, H12, and RH remain open. The persistent RH Goal remains
active.
