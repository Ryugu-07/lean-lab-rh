# H12 Levinson--Montgomery Jensen Top Zero Count

Date: 2026-07-30

Campaign:
`LITERATURE-20260730-H12-LEVINSON-MONTGOMERY-JENSEN-TOP-ZERO-COUNT-01`

Status: `FULL_FIXED_ENDPOINT_SUCCESS / LOCAL_AUDIT_GREEN / PUBLIC_IMPLEMENTATION_PENDING`

## Attempt log

| step | mode | result | next action |
| --- | --- | --- | --- |
| 1 | `H0_PUBLIC_CLOSURE` | The conditional reverse Chebyshev--von Koch inference passed frozen implementation, immutable-evidence, and closure-ledger public CI. The every-positive-epsilon error producer, H0, and RH remain open. | Rerank non-adjacent historical families. |
| 2 | `CROSS_FAMILY_SELECTION` | H1/H2 remain at broad moment producers; H7/H8 at missing concrete spectral/RKHS objects; H10 at absent general-curve geometry; H11 at actual PCC asymptotics. H12 has a precise global source sentence immediately above many compiled local contour components. | Re-read Levinson--Montgomery Theorem 1, page 52. |
| 3 | `PRIMARY_SOURCE_AUDIT` | The source compresses the top argument variation for actual zeta and zeta derivative into a "standard use of Jensen's theorem." It needs actual analytic real-part symmetrizations, polynomial circle bounds, center lower bounds, and a crossing count. | Inventory Jensen and zeta-growth support. |
| 4 | `LEAN_INVENTORY` | Mathlib provides `AnalyticOnNhd.sum_divisor_le`; the project provides Abel zeta bounds, functional-equation Gamma estimates, Cauchy-compatible analyticity, actual multiplicity divisors, and all downstream H12 local contour components. No actual Jensen top-count theorem exists. | Freeze the full actual endpoint before production edits. |
| 5 | `DERIVATIVE_PHASE_AUDIT` | The first derivative Dirichlet term is `-log(2)*2^(-s)` and its real part rotates with height. Multiplication by `exp(i*t*log 2)` fixes that term on the negative real axis. | Require the phase-normalized derivative symmetrization and an unnormalized negative control. |
| 6 | `PREREGISTRATION` | Full success requires actual zeta and derivative analyticity, fixed-strip polynomial bounds, quantitative center separation, Jensen divisor `O(log T)`, and source crossing inclusion. An abstract Jensen wrapper alone is classified as infrastructure. | Publish docs-only preregistration and await public CI. |
| 7 | `PUBLIC_PREREG_GATE` | Commit `90d078eed410ec31a98df0203cc295a5d1967318` passed Lean Action run `30526085450`, build job `90817192932`. | Open the frozen production endpoint. |
| 8 | `ACTUAL_ZETA_GROWTH` | Abel continuation on the right and the functional equation on the bounded left strip give a uniform polynomial bound for actual zeta. The Gamma growth and cosine growth cancel at the exponential scale. | Transfer the bound to the derivative by Cauchy. |
| 9 | `DERIVATIVE_CENTER` | The phase-normalized `n=2` derivative term is exactly `-log(2)/2^20`; the complete tail is at most `2/3^17`, giving a kernel-checked fixed positive center separation. | Apply Jensen to both actual symmetrizations. |
| 10 | `JENSEN_DIVISOR_COUNT` | Both actual symmetrizations are analytic on the fixed outer ball and have multiplicity-bearing inner-ball divisor sums bounded by `C*log(t+2)`. | Compile source crossing inclusion. |
| 11 | `CROSSING_SUPPORT` | Every source real-part crossing on `[0,1]` at `t>=23` lies in the support of the corresponding actual divisor. | Register exact checks, audit axioms, and isolate the next source edge. |

## Resolved obstruction map

1. Actual zeta has the required polynomial growth on the complete outer-circle strip.
2. Fixed-strip Gamma growth and cosine growth cancel before the polynomial estimate is taken.
3. Cauchy's estimate transfers the zeta bound to `deriv riemannZeta` with a fixed margin.
4. Phase normalization gives a quantitative derivative center lower bound.
5. The explicit Jensen quotient simplifies to a logarithmic bound with positive constants.
6. Real crossings enter the support of the multiplicity-bearing complex divisor.

## Local result

The 1552-line module
`LeanLab/Riemann/LevinsonMontgomeryJensenTopZeroCount.lean` completes all seven
preregistered steps for the actual project zeta objects. Seven exact TargetChecks compile.
The seven selected axiom prints depend only on `propext`, `Classical.choice`, and
`Quot.sound`. The campaign forbidden scan and `git diff --check` are empty. Full
`lake build` passes `8815/8815` with inherited warnings only.

Classification:

- `result=LEVINSON_MONTGOMERY_ACTUAL_JENSEN_TOP_ZERO_COUNT_FORMALIZED`;
- `historical_route_coverage_delta=1`;
- `actual_zeta_top_count_delta=1`;
- `actual_zeta_deriv_top_count_delta=1`;
- `argument_variation_delta=0`;
- `levinson_montgomery_count_delta=0`;
- `rh_frontier_delta=0`;
- `rh_proved=0`.

## Next obstruction map

1. Convert ordered real-part crossing counts into a bound on a continuous change of argument
   without assuming endpoint nonvanishing.
2. Produce an admissible cofinal set of top heights, or discharge the complementary
   strict-negative branch.
3. Assemble the finite indented boundary and multiplicity-aware argument principle.
4. Derive the global count identity and the exact
   `N_1^-(T)=N^-(T)+O(log T)` comparison.
5. Compose the completed dichotomy with Speiser's criterion.

## Boundary

The campaign formalizes a known source step. Even full success supplies only the top
zero-count mechanism. The crossing-to-argument theorem, global indented argument principle,
both Levinson--Montgomery count outputs, Speiser equivalence, H12, and RH remain open unless
separately compiled.
