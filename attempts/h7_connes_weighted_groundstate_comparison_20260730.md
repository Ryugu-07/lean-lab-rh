# H7 Connes Weighted Ground-State Comparison Attempt

Campaign:
`PROOF-ATTEMPT-20260730-H7-CONNES-WEIGHTED-GROUNDSTATE-COMPARISON-01`

Node: `H7-CONNES-ACTUAL-GROUNDSTATE-COMPARISON-01`

Mode: `LITERATURE / PROOF-ATTEMPT / FALSIFICATION`

Status:
`MEANINGFUL_PARTIAL / EXACT_CONSUMER_COMPILED / ACTUAL_SOURCE_RATE_LOCAL_STOP`

## Runtime record

- `model`: Codex, GPT-5 family; exact serving variant not exposed.
- `reasoning_effort`: not exposed.
- `loop_budget`: no numerical quota; serving token budget not exposed.
- `compaction_state`: inherited compaction summary was received; canonical governance,
  `HANDOFF.md`, current Targets/TargetChecks, hard-gap DAG, and the relevant H7/H2 records
  were re-read before selection.
- `global_goal`: active.

## Fixed question

Can the literal source-normalized true lowest Weil eigenfunction be compared with Connes'
explicit prolate packet at the support-sensitive rate needed to transfer the packet's
compact-uniform Fourier convergence to `Xi`?

## Loop ledger

| loop | mode | result | decision |
| --- | --- | --- | --- |
| 1 | `ROUTE_SELECTION / SOURCE_AUDIT` | Re-read the current H2 stop, H7 topology result, Rayleigh-gap consumer, Targets, DAG, and the TeX sources of arXiv:2602.04022 and 2511.22755. The source proves `Fourier(k_lambda)->Xi` but leaves the true-ground comparison and simple-even structure open. | Select the actual weighted comparison over H12, H10, H11, H1, adjacent H2, and H8. |
| 2 | `RATE_DERIVATION / FALSIFICATION_DESIGN` | Source support becomes `[-log(lambda),log(lambda)]`. Weighted Cauchy--Schwarz costs about `lambda^A`; therefore squared projective error must be `o(lambda^(-2*A))`. Existing counterexamples already reject unweighted convergence and absolute excess. | Preregister the literal scaled Rayleigh-excess/gap rate. Do not count an abstract conditional theorem as full success. |
| 3 | `PROOF-ATTEMPT / LEAN` | Lean proves the exact positive-strip squared weight mass `(exp(2*A*R)-1)/A`, the endpoint mass `2*R`, polarization, and oriented normalized `L2Error <= 2*projectiveDefect`. | Continue to the sequence producer with no extra `sqrt(log(lambda))` loss for `A>0`. |
| 4 | `PROOF-ATTEMPT / LEAN` | Lean proves that `lambda^(2*A)*ratio -> 0` for all `0<A<1/2` implies the complete weighted comparison for all `0<=A<1/2`; the endpoint follows from `log(lambda)<=lambda^epsilon/epsilon`. The existing Fourier transfer then consumes any separately compiled packet limit. | Register only a conditional consumer; criterion 4 remains open. |
| 5 | `LITERATURE / SOURCE_AUDIT` | Re-read Connes 2026 Sections 6.4--6.6 and Connes--Consani 2023 Sections 2 and 3 from source TeX. The latter gives numerical graph agreement and uses approximate equalities, while reporting roughly `lambda^2` simultaneous minuscule eigenvalues. It proves no packet excess, first gap, or excess/gap rate. | Record the exact source-availability obstruction. Do not infer alignment from a tiny Rayleigh value. |
| 6 | `FALSIFICATION / LEAN` | For `lambda_n=exp(n)` and a two-dimensional family with gap and packet excess both `exp(-n)`, Lean proves every support-weighted absolute-excess rate for `A<1/2`, but the excess/gap ratio and projective defect are exactly one. | Reject absolute-excess promotion. Locally stop the actual source-rate attack until a new producer controls the ratio or an approximate-commutator/joint-spectral mechanism bypasses the tiny internal gap. |

## Frozen frontier before production

- `compiled_left`: exact source coordinate alignment; finite matrix/parity interface; actual
  pole and finite-prime blocks; archimedean density; finite dictionary; Rayleigh-gap consumer;
  weighted Fourier-strip transfer.
- `compiled_negative_controls`: collapsing spectral-gap family and smooth escaping-support
  packets.
- `new_compiled_consumer`: exact support-sensitive Hölder estimates, coherent orientation,
  `lambda^(2*A)` rate transfer including `A=0`, and packet-to-ground Fourier transfer.
- `new_compiled_negative_control`: even an absolute excess with every required
  `lambda^(2*A)` rate does not align eigenvectors when the gap collapses at the same rate.
- `exact_open_producer`: for every `0<A<1/2`,
  `lambda^(2*A)*RayleighExcess(sourceProlate)/sourceGroundGap -> 0`, with coherent source
  normalization and the `A=0` endpoint.
- `known_but_uncompiled_input`: the source `Fourier(k_lambda)->Xi` closed-substrip theorem
  cannot be used downstream until reconstructed and audited in Lean.
- `independent_open_edge`: simple isolated even ground-state structure.
- `not_claimed`: actual comparison, transform convergence for true ground states, all-real
  limiting zeros, H7, or RH.
- `source_obstruction`: the audited primary sources supply numerical comparison and prolate
  angle asymptotics, but no source theorem bounding the actual Weil packet excess, the first
  Weil spectral gap, or their ratio.  The growing cluster of minuscule eigenvalues makes this
  omission mathematically material.
- `possible_H7_reentry`: derive an actual total-operator estimate, or prove that the Weil
  operator approximately commutes with a prolate label operator whose separated spectrum can
  identify `k_lambda` without dividing by the tiny internal Weil gap.
- `next_action`: publish the meaningful-partial implementation and local-stop ledger, then
  perform cross-family route selection rather than optimizing constants inside this consumer.

The persistent RH Goal remains active.
